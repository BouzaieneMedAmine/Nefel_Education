// this var can get a value of arry, number, srting ... - what would this variable do?
var likeCount = 0;
// it will excute when we call it - what would be the function of this code block?
function increaseLikes() {
    // we add +1 to the var likeCount - what is this sort operation for?
    likeCount = likeCount + 1;
}
